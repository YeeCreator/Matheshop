export type CanvasCellSelectionOutlineProps = {
  isSelected: boolean
}

export default function CanvasCellSelectionOutline(_props: CanvasCellSelectionOutlineProps) {
  // 当前样式由 `.cell.is-selected` 驱动；此组件作为后续增强（更复杂 outline/多选框）预留。
  return null
}

