export type CanvasCellDropHintProps = {
  isDropHint: boolean
}

export default function CanvasCellDropHint(_props: CanvasCellDropHintProps) {
  // 当前样式由 `.cell.is-drop-hint` 驱动；此组件作为后续增强（例如显示插入线/提示标签）预留。
  return null
}

