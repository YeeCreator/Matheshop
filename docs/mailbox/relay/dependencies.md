# mailbox/relay dependencies: matheshop

This project depends on the following governed local projects.

- `main-ui`: write feedback in `feedback/outbox/main-ui/`.
- `viewport-2d-kit`: write feedback in `feedback/outbox/viewport-2d-kit/`.
