# engine_ts（TypeScript 计算引擎，占位）

目标：提供一个 **TypeScript 版本** 的自研符号/算术计算引擎的最小实现，用于简化前后端模块设计。

当前状态：仅建立目录与文档占位；暂不包含实现代码。

后续建议（等你确认需求后再做）：
- 设计 TS AST 与化简/求值 API（与 `src/symbolic/*` 对齐或复用）
- 增加最小测试集（表达式解析/化简/求值）
- 决定运行形态：
  - 纯前端内置（浏览器/Worker）
  - Node 服务（对标现有 Python FastAPI）
  - 同构库（前后端共享）

